specVersion: "4.0"
metadata:
  name: project1
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          client: "100"
        exclude:
          - /test/
          - /localService/
